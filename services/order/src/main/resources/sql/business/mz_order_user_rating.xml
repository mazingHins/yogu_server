<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.order.business.dao.OrderUserRatingDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.order.business.entry.OrderUserRatingPO" useGeneratedKeys="false" >
		insert into mz_order.mz_order_user_rating (order_id,store_id,uid,evaluator,star,create_time) values (#{orderId},#{storeId},#{uid},#{evaluator},#{star},#{createTime})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_order.mz_order_user_rating WHERE order_id=#{param1} AND uid=#{param2}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.order.business.entry.OrderUserRatingPO">
		update mz_order.mz_order_user_rating set store_id=#{storeId},evaluator=#{evaluator},star=#{star} where order_id=#{orderId} and uid=#{uid}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.order.business.entry.OrderUserRatingPO">
		SELECT * FROM mz_order.mz_order_user_rating WHERE order_id=#{param1} AND uid=#{param2}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.order.business.entry.OrderUserRatingPO">
		SELECT * FROM mz_order.mz_order_user_rating
	</select>
	
	<!-- 根据订单Id查询记录 (Id可能有多个) -->
	<select id="getByOrderIds" resultType="com.mazing.services.order.business.entry.OrderUserRatingPO">
		SELECT * FROM mz_order.mz_order_user_rating where order_id IN (
			<foreach collection="orderIds" item="item" index="index" separator="," >#{item,jdbcType=BIGINT}</foreach>
		)
	</select>
	
	<select id="listByOrderId" parameterType="Map" resultType="com.mazing.services.order.business.entry.OrderUserRatingPO">
		SELECT * FROM mz_order.mz_order_user_rating WHERE order_id=#{orderId} 
	</select>

</mapper>
