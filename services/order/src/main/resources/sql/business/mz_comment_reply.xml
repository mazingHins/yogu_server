<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.order.business.dao.CommentReplyDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.order.business.entry.CommentReplyPO" useGeneratedKeys="false" >
		insert into mz_order.mz_comment_reply (reply_id,comment_id,sender,content,status,create_time) values (#{replyId},#{commentId},#{sender},#{content},#{status},#{createTime})
	</insert>

	<!-- 根据主键，删除数据 -->
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_order.mz_comment_reply WHERE reply_id=#{replyId}
	</delete>

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.order.business.entry.CommentReplyPO">
		update mz_order.mz_comment_reply set comment_id=#{commentId},sender=#{sender},content=#{content},status=#{status} where reply_id=#{replyId}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.order.business.entry.CommentReplyPO">
		SELECT * FROM mz_order.mz_comment_reply WHERE reply_id=#{replyId}
	</select>

	<!-- 根据评论ID读取记录 -->
	<select id="getByCommentId" parameterType="java.lang.Long" resultType="com.mazing.services.order.business.entry.CommentReplyPO">
		SELECT * FROM mz_order.mz_comment_reply WHERE comment_id=#{commentId} limit 0,1;
	</select>

</mapper>
