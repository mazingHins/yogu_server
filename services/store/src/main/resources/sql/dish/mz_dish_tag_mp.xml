<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.dish.dao.DishTagMpDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.dish.entry.DishTagMpPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_dish_tag_mp (dish_key,tag_id,create_time) values (#{dishKey},#{tagId},#{createTime})
	</insert>

	<!-- 根据主键，删除数据 -->
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_dish_tag_mp WHERE dish_key=#{dishKey} AND tag_id=#{tagId}
	</delete>

	<!-- 清除某个菜品的全部配置 -->
	<delete id="deleteByDishKey" parameterType="Map">
		DELETE FROM mz_merchant.mz_dish_tag_mp WHERE dish_key=#{dishKey}
	</delete>

	<!-- 查询某个菜品（dishKey）的全部tag配置 -->
	<select id="queryByDishKey" parameterType="Map" resultType="com.mazing.services.store.dish.entry.DishTagMpPO">
		SELECT * FROM mz_merchant.mz_dish_tag_mp WHERE dish_key=#{dishKey}
	</select>

</mapper>
