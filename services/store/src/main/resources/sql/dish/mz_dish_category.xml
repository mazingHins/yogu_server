<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.dish.dao.DishCategoryDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.dish.entry.DishCategoryPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_dish_category (category_id,category_name,create_time) values (#{categoryId},#{categoryName},#{createTime})
	</insert>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.dish.entry.DishCategoryPO">
		SELECT * FROM mz_merchant.mz_dish_category WHERE category_id=#{categoryId}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.dish.entry.DishCategoryPO">
		SELECT * FROM mz_merchant.mz_dish_category
	</select>
	
	<!-- 修改美食分类名称 -->
	<update id="updateName" parameterType="Map">
		update mz_merchant.mz_dish_category set category_name = #{name} where category_id=#{categoryId}
	</update>

</mapper>
