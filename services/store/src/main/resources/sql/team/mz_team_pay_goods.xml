<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.base.dao.TeamPayGoodsDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.base.entry.TeamPayGoodsPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_team_pay_goods (goods_id,image,name_cn,name_en,status,create_time,update_time) values (#{goodsId},#{image},#{nameCn},#{nameEn},#{status},#{createTime},#{updateTime})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_team_pay_goods WHERE goods_id=#{param1}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.base.entry.TeamPayGoodsPO">
		update mz_merchant.mz_team_pay_goods set image=#{image},name_cn=#{nameCn},name_en=#{nameEn},total=#{total},status=#{status},update_time=#{updateTime} where goods_id=#{goodsId}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.base.entry.TeamPayGoodsPO">
		SELECT * FROM mz_merchant.mz_team_pay_goods WHERE goods_id=#{param1}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.base.entry.TeamPayGoodsPO">
		SELECT * FROM mz_merchant.mz_team_pay_goods
	</select>

</mapper>
