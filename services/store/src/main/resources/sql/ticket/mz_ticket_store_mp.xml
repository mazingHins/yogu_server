<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.ticket.dao.TicketStoreMpDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.ticket.entry.TicketStoreMpPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_ticket_store_mp (id,act_id,ticket_rule_id,store_id,add_time) values (#{id},#{actId},#{ticketRuleId},#{storeId},#{addTime})
	</insert>

	<!-- 根据主键，删除数据 -->
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_ticket_store_mp WHERE id=#{param1}
	</delete>
	

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.ticket.entry.TicketStoreMpPO">
		update mz_merchant.mz_ticket_store_mp set act_id=#{actId},ticket_rule_id=#{ticketRuleId},store_id=#{storeId},add_time=#{addTime} where id=#{id}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.ticket.entry.TicketStoreMpPO">
		SELECT * FROM mz_merchant.mz_ticket_store_mp WHERE id=#{param1}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.ticket.entry.TicketStoreMpPO">
		SELECT * FROM mz_merchant.mz_ticket_store_mp
	</select>

	<select id="listByActId" resultType="com.mazing.services.store.ticket.entry.TicketStoreMpPO">
		SELECT * FROM mz_merchant.mz_ticket_store_mp where act_id=#{actId}
	</select>

	<select id="listByTicketRuleId" resultType="com.mazing.services.store.ticket.entry.TicketStoreMpPO">
		SELECT * FROM mz_merchant.mz_ticket_store_mp where ticket_rule_id=#{ticketRuleId}
	</select>

	
</mapper>
