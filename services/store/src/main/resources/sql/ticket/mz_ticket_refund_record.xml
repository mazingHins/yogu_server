<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.ticket.dao.TicketRefundRecordDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.ticket.entry.TicketRefundRecordPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_ticket_refund_record (rid,order_no,uid,refund_time,refund_type,refund_status,ticket_ids) values (#{rid},#{orderNo},#{uid},#{refundTime},#{refundType},#{refundStatus},#{ticketIds})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_ticket_refund_record WHERE rid=#{param1}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.ticket.entry.TicketRefundRecordPO">
		update mz_merchant.mz_ticket_refund_record set order_no=#{orderNo},uid=#{uid},refund_time=#{refundTime},refund_type=#{refundType},refund_status=#{refundStatus},ticket_ids=#{ticketIds} where rid=#{rid}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.ticket.entry.TicketRefundRecordPO">
		SELECT * FROM mz_merchant.mz_ticket_refund_record WHERE rid=#{param1}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.ticket.entry.TicketRefundRecordPO">
		SELECT * FROM mz_merchant.mz_ticket_refund_record
	</select>

</mapper>
