<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.ticket.dao.EventStoreMpDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.ticket.entry.EventStoreMpPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_event_store_mp (event_id,store_id,create_time) values (#{eventId},#{storeId},#{createTime})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_event_store_mp WHERE event_id=#{param1} AND store_id=#{param2}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.ticket.entry.EventStoreMpPO">
		update mz_merchant.mz_event_store_mp set  where event_id=#{eventId} and store_id=#{storeId}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.ticket.entry.EventStoreMpPO">
		SELECT * FROM mz_merchant.mz_event_store_mp WHERE event_id=#{param1} AND store_id=#{param2}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.ticket.entry.EventStoreMpPO">
		SELECT * FROM mz_merchant.mz_event_store_mp
	</select>

</mapper>
