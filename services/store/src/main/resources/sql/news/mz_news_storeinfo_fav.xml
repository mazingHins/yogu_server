<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.news.dao.NewsStoreinfoFavDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_news_storeinfo_fav (fid,uid,sinfo_id,type) values (#{fid},#{uid},#{sinfoId},#{type})
	</insert>

	<!-- 根据主键，删除数据 -->
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_news_storeinfo_fav WHERE fid=#{param1}
	</delete>
	

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO">
		update mz_merchant.mz_news_storeinfo_fav set uid=#{uid},sinfo_id=#{sinfoId},type=#{type} where fid=#{fid}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_fav WHERE fid=#{param1}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_fav
	</select>

	
	<!-- 根据uid 和 type类型查询用户的 收藏餐厅详情信息 -->
	<select id="listByUid" resultType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_fav where uid=#{uid} AND type=#{type}
	</select>
	
	<select id="listByUidAndSinfoId" resultType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_fav where uid=#{uid} AND sinfo_id=#{sinfoId}
	</select>
	
	
	<!-- 根据type获取所有去重的uid集合 -->
	<select id="listUserFavStoreCount" parameterType="Map" resultType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO">
		SELECT uid, count(sinfo_id) as sinfo_id FROM mz_merchant.mz_news_storeinfo_fav where type=#{type} GROUP BY uid LIMIT #{offset},#{rows}
	</select>
	
	
	<select id="getByUniqKey" parameterType="Map" resultType="com.mazing.services.store.news.entry.NewsStoreinfoFavPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_fav WHERE uid=#{uid} AND sinfo_id=#{sinfoId} AND type=#{type}
	</select>

</mapper>
