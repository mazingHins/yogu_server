<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.invite.dao.StoreInviteCodeDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.invite.entry.StoreInviteCodePO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_store_invite_code (invite_code,uid,use_time,effective,create_time) values (#{inviteCode},#{uid},#{useTime},#{effective},#{createTime})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_store_invite_code WHERE invite_code=#{param1}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.invite.entry.StoreInviteCodePO">
		update mz_merchant.mz_store_invite_code set uid=#{uid},use_time=#{useTime},effective=#{effective} where invite_code=#{inviteCode}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.invite.entry.StoreInviteCodePO">
		SELECT * FROM mz_merchant.mz_store_invite_code WHERE invite_code=#{param1}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.invite.entry.StoreInviteCodePO">
		SELECT * FROM mz_merchant.mz_store_invite_code
	</select>

</mapper>
