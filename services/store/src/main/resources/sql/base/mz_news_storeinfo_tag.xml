<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.base.dao.NewsStoreinfoTagDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.base.entry.NewsStoreinfoTagPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_news_storeinfo_tag (sinfo_id,tag_id,create_time) values (#{sinfoId},#{tagId},#{createTime})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_news_storeinfo_tag WHERE sinfo_id=#{param1} AND tag_id=#{param2}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.base.entry.NewsStoreinfoTagPO">
		update mz_merchant.mz_news_storeinfo_tag set  where sinfo_id=#{sinfoId} and tag_id=#{tagId}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.base.entry.NewsStoreinfoTagPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_tag WHERE sinfo_id=#{param1} AND tag_id=#{param2}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.base.entry.NewsStoreinfoTagPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_tag
	</select>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="listBySinfoId" parameterType="Map" resultType="com.mazing.services.store.base.entry.NewsStoreinfoTagPO">
		SELECT * FROM mz_merchant.mz_news_storeinfo_tag WHERE sinfo_id=#{sinfoId}
	</select>
</mapper>
