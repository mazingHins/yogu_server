<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.base.dao.MerchantOperationLogDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.base.entry.MerchantOperationLogPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_merchant_operation_log (log_id,store_id,uid,type,module,content,create_time) values (#{logId},#{storeId},#{uid},#{type},#{module},#{content},#{createTime})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_merchant_operation_log WHERE log_id=#{param1}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.base.entry.MerchantOperationLogPO">
		update mz_merchant.mz_merchant_operation_log set store_id=#{storeId},uid=#{uid},type=#{type},module=#{module},content=#{content} where log_id=#{logId}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.base.entry.MerchantOperationLogPO">
		SELECT * FROM mz_merchant.mz_merchant_operation_log WHERE log_id=#{param1}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.base.entry.MerchantOperationLogPO">
		SELECT * FROM mz_merchant.mz_merchant_operation_log
	</select>

</mapper>
