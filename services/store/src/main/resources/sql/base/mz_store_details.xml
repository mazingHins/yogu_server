<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 这里namespace必须是接口的路径，不然要运行的时候要报错 “is not known to the MapperRegistry” -->
<mapper namespace="com.mazing.services.store.base.dao.StoreDetailsDao">

	<!-- 新增 -->
	<insert id="save" parameterType="com.mazing.services.store.base.entry.StoreDetailsPO" useGeneratedKeys="false" >
		insert into mz_merchant.mz_store_details (store_id,superior_quality,verified,have_safety_insurance,content,create_time) values (#{storeId},#{superiorQuality},#{verified},#{haveSafetyInsurance},#{content},#{createTime})
	</insert>

	<!-- 根据主键，删除数据
	<delete id="deleteById" parameterType="Map">
		DELETE FROM mz_merchant.mz_store_details WHERE store_id=#{storeId}
	</delete>
	 -->

	<!-- 更新 -->
	<update id="update" parameterType="com.mazing.services.store.base.entry.StoreDetailsPO">
		update mz_merchant.mz_store_details set superior_quality=#{superiorQuality},verified=#{verified},have_safety_insurance=#{haveSafetyInsurance},content=#{content} where store_id=#{storeId}
	</update>

	<!-- 根据主键读取记录（主键可能有多个） -->
	<select id="getById" parameterType="Map" resultType="com.mazing.services.store.base.entry.StoreDetailsPO">
		SELECT * FROM mz_merchant.mz_store_details WHERE store_id=#{storeId}
	</select>

	<!-- 查询全部记录 -->
	<select id="listAll" resultType="com.mazing.services.store.base.entry.StoreDetailsPO">
		SELECT * FROM mz_merchant.mz_store_details
	</select>

</mapper>
